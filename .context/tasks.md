# Tasks

Task IDs are ULIDs; keep titles short and human-readable.

## Active
- id: 01KQ9AGENTINSTRUCTIONS2026 — title: Build agent instruction infrastructure — owner: Anthony/Codex — status: active — last update: 2026-05-08 — note: Split authoritative cached vendor docs under `agents/official-docs` from model guidance under `agents/models`; copied web-doc exports remain distinct from optional pinned reference submodules for Codex source, Anthropic skills, Cursor plugins, and Gemini CLI. Skill packages now separate canonical source directories under `agents/skills/src/**/<skill>/`; `SKILL.md` frontmatter is the canonical skill identity source, with no separate source `metadata.yaml`. Source skills may include optional `model-notes/<model>.md`, optional `harness-notes/<harness>.md`, and optional `evals/`; matching notes and eval fixtures participate in `update-skill.bash` prompts and input digests only when present. Prompt packages now separate canonical source under `agents/prompts/src/**/<prompt>/` plus optional prompt-specific `harness-notes/<harness>.md`; matching prompt harness notes participate in `update-prompt.bash` prompts and input digests only when present. Shared artifact-production rules belong in canonical source prompts/skills, while notes should remain target-specific deltas. Runtime artifacts under `agents/skills/artifacts/`, prompt artifacts under `agents/prompts/harnesses/`, and digest stamps under `.update-stamps/` are present for the current targets after updater-script regeneration; do not manually bootstrap, patch, or stamp artifacts. Source skills and prompts may be organized into arbitrary subdirectories, but their directory names must be globally unique within each source tree. Public update runners are `agents/scripts/update-skill.bash`, `agents/scripts/update-prompt.bash`, and `agents/scripts/update-all.bash`; skill/prompt runners support first-pass `--force`, and `update-all.bash` defaults to skill mode while supporting `--type prompt` and `--prompt`. Symlink runners are `agents/scripts/symlink-skill.bash` and `agents/scripts/symlink-all.bash`; they discover configured home targets from harness `home_config` files using each harness's explicit `model_config_key` unless `--harness --model` is explicit, and `symlink-all.bash [skill-prefix]` deploys only source skills under a selected `skills/src/` subtree. Model guides resolve directly from `agents/models/<model>.md`; no provider-prefix mapping is maintained in Bash, while harness-native model aliases such as Claude Code's `best`/`opus` live in harness YAML and normalize to canonical artifact ids. Supported harnesses are discovered from `agents/harnesses/<harness>.yaml`, where `<harness>` matches the executable name and records home config paths, skill install paths, model config keys, model aliases, output paths, and structural runner args. Missing native target executables are handled per selected artifact with skip/fallback policy. Latest local verification after rebasing `main` onto `origin/main` passed on 2026-05-08 with the live home re-bootstrapped for the new agent symlink targets.
- id: 01KQ9AGENTHARNESSVARIANTS2026 — title: Add all agent harness artifacts — owner: Anthony/Codex — status: active — last update: 2026-05-04 — note: Codex, Claude Code, Cursor Agent, and Gemini CLI now have harness YAML/docs and `commit-prep` artifact targets. Native production runs use each local harness configuration as the authoring model and do not pass target artifact model ids as CLI overrides. Claude Code aliases `best` and `opus` normalize to `claude-opus-4-7` through `agents/harnesses/claude.yaml`; Cursor Agent maps documented `auto` and self-repaired `default` to the canonical `composer-2-fast` artifact target and passes `--model auto` because Free-plan headless production rejects named/default model selection; add other harness aliases only when official docs or live CLI behavior expose stable alias behavior and corresponding artifacts exist. Gemini uses `model_config_key: model.name`, deploys under `.gemini/skills` to avoid clobbering Codex's `.agents/skills`, documents the same-name shadowing caveat, and keeps task prose in prompt artifacts rather than hardcoded YAML `runner_args`. Runtime artifacts and digest stamps are present after updater-script regeneration.
- id: 01KPNZ3YBAKB9N4ZJEXW4P2EHP — title: Migrate shell infrastructure to zsh — owner: Anthony/Codex — status: active — last update: 2026-05-08 — note: POSIX baseline + bootstrap simplification checkpoint reached; zsh now has a native prompt/tool-support layer, fzf integration guarded by shell-generator capability, line-editor/history defaults, shared timestamp history policy, selected zsh/complist completion presentation, and deeper vi-mode helpers. The shared `-` alias now feature-detects the bash/zsh `alias --` form and falls back to dash-compatible syntax so `/bin/sh` startup stays quiet. Non-clipboard vi mode is merged; clipboard/register unification is the current shell-adjacent slice.
- id: 01KQ6VIMODECLIPBOARD2026 — title: Unify clipboard behavior — owner: Anthony/Codex — status: active — last update: 2026-04-28 — note: Clipboard v1 targets macOS and WSL/Windows only. The shared wrapper API is `dotfiles-clipboard copy|paste|status`; macOS uses `pbcopy`/`pbpaste`, and WSL now uses Microsoft-native `clip.exe` for copy plus PowerShell `Get-Clipboard -Raw` for paste with CR stripping centralized in the wrapper. zsh yank/delete/change/substitute CUTBUFFER operations, paste-time `CUTBUFFER` refresh, visual selection paste, and tmux copy/import/paste via `dotfiles-clipboard-tmux` are implemented for v1 with quiet unsupported-provider fallbacks. Next smoke-test behavior under real WSL. OSC 52 and Linux desktop providers are deferred.
- id: 01KQ9WINTERMSOLARIZED2026 — title: Add Windows Terminal Solarized Dark — owner: Anthony/Codex — status: active — last update: 2026-04-28 — note: Continue on the Windows/WSL machine. Inspect live Windows Terminal `settings.json` before deciding between tracking a full file, applying a merge helper, or documenting a manual fragment. Preferred first design is a named Solarized Dark scheme plus `profiles.defaults.colorScheme`, preserving generated profiles/GUIDs and WSL distro entries. Coordinate with larger WSL clipboard fixes, then return to macOS and rerun full verification.

## Paused / Blocked
- id: 01KPZR91C2GZB5GKM9Z9GQP8EK — title: Evaluate selective Bash ergonomics from Oh My Bash — owner: Anthony/Codex — status: planned — last update: 2026-04-26 — note: Mostly complete: history defaults, timestamped history, Bash `checkwinsize`, system-first Bash/zsh tool support, fzf shell support, prefix history search, and case-/hyphen-insensitive completion matching have all been selected. Still consider small explicit directory navigation helpers as standalone follow-ups. Do not adopt Oh My Bash wholesale; zoxide and `HISTCONTROL=ignoredups` are explicitly declined.
- id: 01KQ3XW4XXCF3J4R8FXD916CEM — title: Port Bash upgrades to idiomatic zsh — owner: Anthony/Codex — status: planned — last update: 2026-04-26 — note: After the Oh My Bash-inspired Bash pass, evaluate which improvements should also exist in zsh and implement them using zsh-native mechanisms rather than Bash compatibility shims.
- id: 01KQ3XW4XX02RMQ7TW36T5942Y — title: Evaluate Oh My Zsh inspiration — owner: Anthony/Codex — status: planned — last update: 2026-04-26 — note: Compare Oh My Zsh against this repo's zsh layer for selective ideas around completion, prompt, history, plugins, and ergonomics. Initial Homebrew zsh helper candidates such as autosuggestions, syntax highlighting, zsh-completions, and autocomplete were reviewed and declined for now. Do not adopt the framework wholesale without a separate decision.
- id: 01KQ3XW4XXE0YD3WJ06V3EQ195 — title: Re-evaluate settings scripts — owner: Anthony/Codex — status: planned — last update: 2026-04-26 — note: Review the whole `settings/` section for current macOS/Git defaults, stale assumptions, idempotency, docs, and whether any settings should move into clearer shell/bootstrap flows.
- id: 01KPNZ3YB7K74CJXMSM8HD9VQR — title: Write repo documentation pass — owner: Anthony/Codex — status: planned — last update: 2026-04-20
- id: 01KPNZ4YNEOVIMEVALSTEP2026 — title: Evaluate Neovim after Vim migration stabilizes — owner: Anthony — status: planned — last update: 2026-04-21 — note: Optional future work; vanilla Vim remains the portable SSH baseline unless you deliberately switch.
- id: 01KQ8TPMPLUGINSEVAL2026 — title: Evaluate tmux plugins after Neovim — owner: Anthony/Codex — status: planned — last update: 2026-04-28 — note: Revisit TPM only after Neovim has been considered. Initial plugin candidates are `tmux-resurrect` and `tmux-continuum`; do not introduce a tmux plugin manager during the current clipboard/tmux config pass.
- id: 01KQ8TMUXSELMODEBUG2026 — title: Address tmux selection-mode line bug upstream — owner: Anthony/Codex — status: planned — last update: 2026-04-28 — note: Do this at the very end of the project after tmux plugin evaluation. Full GitHub issue draft is in `.context/scratch/20260428-tmux-selection-mode-line-bug/report.md`; it documents `selection-mode line` lazily applying and clobbering the anchor row on tmux 3.6a and master. Current repo workaround uses `select-line` only for fresh `V` entry and accepts broken mid-selection `V` switches until upstream is fixed. When fixed, mid-selection should work with no config change; fresh `V` can optionally migrate back to `begin-selection ; rectangle-off ; selection-mode line` for symmetry and cursor-column preservation.

## Completed (recent; keep last ~10)
- id: 01KPVT9N992M71VGJVBXDATR7B — title: Review tmux configuration — owner: Anthony/Codex — status: done — last update: 2026-04-28 — note: Tmux config pass completed for now: syntax reload verified on tmux 3.6a; TERM/RGB/focus/status/window-option syntax modernized; plain `tmux` in interactive shells attaches/creates session `default`; new panes prefer interactive login zsh then interactive login Bash then tmux's default login shell; vi prompt/copy-mode keys are explicit, including Vim-like `v`/`V`/`C-v` selection toggles; custom bindings have `bind -N` help notes; custom windows/splits preserve pane cwd; top/bottom split moved to prefix `-` while prefix `s` remains session chooser; prefix `R` reloads config; prefix `C-b` intentionally maps to `last-window` over nested tmux support; clipboard remains routed through `dotfiles-clipboard` rather than tmux `set-clipboard`/OSC 52. TPM/plugin evaluation is deferred until after Neovim.
- id: 01KPR9WB7K84CJXMSM8HD9VQRX — title: Re-evaluate git submodules and CLI utility contents — owner: Anthony/Codex — status: done — last update: 2026-05-03 — note: Removed runtime submodules for `make-chrome-app` and `vim-plug`; vim-plug remains through a tracked `home/.vim/autoload/plug.vim` snapshot. A narrow reference-doc exception now exists for the optional Gemini CLI docs submodule under `agents/official-docs/gemini-cli`.
- id: 01KQ7V7M8ZDQK4WJ9N2P6R3S5T — title: Re-evaluate vim-plug submodule — owner: Anthony/Codex — status: done — last update: 2026-04-27 — note: Removed `lib/vim-plug` as a submodule while keeping vim-plug itself as the plugin manager via a tracked `home/.vim/autoload/plug.vim` snapshot. `:PlugUpgrade` updates that tracked file through the managed symlink.
- id: 01KPWZ6ZQPJHEE3YM1BYVPG679 — title: Do an XDG integration pass — owner: Anthony/Codex — status: done — last update: 2026-04-23 — note: Defaulted shell runtime to XDG base dirs where it makes sense: added `shell/paths.sh` to export `XDG_*` defaults plus internal `dotfiles_*` path vars, sourced managed shell config through that layer, and stored bash/zsh history under `XDG_STATE_HOME`.
- id: 01KPNC22ZGDW3M9SGG6GNTYNSW — title: Migrate Vim from Vundle to vim-plug + plugin refresh — owner: Anthony/Codex — status: done — last update: 2026-04-21 — note: vim-plug + lib submodule; upgraded repos; drop language plugins + capslock done earlier.
- id: 01KPNC22ZK4F5TBEQJ3QYXQXMK — title: Review non-portable Vim colors — owner: Anthony/Codex — status: done — last update: 2026-04-21 — note: Solarized 8 + termguicolors; tmux RGB passthrough; SSH fallback documented in knowledge/vim.md
- id: 01KPNZ3YB3YV5H4SW4NDFDRRE7 — title: Split Codex rules into curated global file — owner: Anthony/Codex — status: done — last update: 2026-04-20
- id: 01KPNCXYF9JGX5D6DPZE2FDHB7 — title: Add lightweight bootstrap verification — owner: Codex (model: gpt-5.4) — status: done — last update: 2026-04-20
- id: 01KPMQQMJB1875SVN6FMD2RVMG — title: Replace chezmoi with repo-native bootstrap — owner: Codex (model: gpt-5.4) — status: done — last update: 2026-04-24 — note: Dropped chezmoi entirely, switched `home/` to a literal target tree with real symlink nodes, moved the whole-directory Vim tree under `managed/vim`, and rewrote bootstrap/verify around a shared repo-native home-tree manifest helper.
- id: 01KP3BK8CKJMEZMC402YYVPRVJ — title: Strengthen global notebook and commit rules — owner: Codex (model: gpt-5.2-codex) — status: done — last update: 2026-04-13
- id: 01KP3BBZ7QM352FJBYPXHES4D8 — title: Lighten code_template docs scaffold — owner: Codex (model: gpt-5.2-codex) — status: done — last update: 2026-04-13
- id: 01KP3B51BKMYS0GBYT6Z7921BX — title: Fold repo reference notes into knowledge — owner: Codex (model: gpt-5.2-codex) — status: done — last update: 2026-04-13
- id: 01KP39VX2MRPFN9W61ZF4TFAZ4 — title: Merge repo user_shared into scratch and reorder decisions — owner: Codex (model: gpt-5.2-codex) — status: done — last update: 2026-04-13
